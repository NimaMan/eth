# Mempool Exit Diagnostics

Set name: `mempool-live-exits`

Diagnostic live set for comparing liquidity-removal and critical-LP exit
variants across several pool-update hold windows. This is intentionally not the
home of default live exits; those defaults live in the shared live strategy
spec.

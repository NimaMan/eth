# LP Approval Warning

Wraps the legacy LP approval warning strategy as a live strategy set with one
strategy. This keeps compatibility for callers that still use the older LP
approval warning set or strategy name.

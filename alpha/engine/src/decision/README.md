# Decision

Decision owns strategy decision recording.

- `record.rs`: converts strategy decisions into durable store records.
- `persistence.rs`: writes market, risk, and position-monitor decisions through
  the engine store.

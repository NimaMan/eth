# Clusters

Actor clusters that cut across individual scammer cases.

Use one folder per cluster, named by the most stable high-signal identifier,
such as creator wallet, forwarder contract, repeated remover, or sink family.

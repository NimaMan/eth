# Detector Prototypes

Actor-level detector notes before promotion into Rust source modules.

Initial candidates:

- repeated creator lifecycle;
- same bytecode/name/symbol token staging;
- LP approval followed by direct creator removal;
- CREATE/selfdestruct forwarder fanout;
- repeated remover or final-sink reuse.

# V4 Vault Simulations

Store V4 route rehearsals and reports here before deployment.

Expected structure:

- `route-fixtures/`: pinned token, pool key, hooks, commands, amounts, and block.
- `reports/`: direct Universal Router and candidate vault simulation outputs.

Every report must record block number, route inputs, calldata, gas policy, and
whether hooks were present.

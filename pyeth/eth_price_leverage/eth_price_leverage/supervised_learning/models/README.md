# Models

Reusable supervised model implementations, abstractions, and configuration helpers. Examples include:
- feature preprocessing pipelines tied to information schema fields
- model wrappers (tree ensembles, linear models, neural nets) with consistent fit/predict interfaces
- hyperparameter search utilities and serialization helpers for deployment

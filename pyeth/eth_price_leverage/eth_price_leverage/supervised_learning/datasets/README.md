# Datasets

Defines how information snapshots are aligned with targets to produce supervised training datasets. Typical components:
- dataset builders that join information windows with labelled outcomes
- schema validators and statistical summaries for QA
- sampling strategies (time-based splits, rolling windows, class balancing)

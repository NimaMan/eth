# Crypto Workspace

## Shared Config

Node and local tooling defaults live in `config.env` at this repository root.
Exported environment variables with the same names override the file.

Current Reth datadir:

```bash
/home/nima/storage/samsung8tb/ethereum/reth
```

# V2 Mode A Gas Benchmark 2026-05-19

Contract: `BaygusTradingVault`

Mode A:

```text
buy:
  ETH -> token through the vault
  no sell-router allowance after buy

emergency sell:
  approve exact token amount
  sell token -> ETH
  clear allowance
  send ETH to treasury
```

## Commands Run

```bash
cd /home/nima/code/crypto/blockchains/eth/solidity/baygus-executor/contracts
forge fmt --check
forge build
forge test --match-contract BaygusTradingVaultGasTest --gas-report
forge snapshot --match-contract BaygusTradingVaultGasTest \
  --snap ../benchmarks/snapshots/v2-mode-a-local.gas-snapshot
forge test --match-contract BaygusTradingVaultForkGasTest \
  --fork-url "$RPC" \
  --fork-block-number 25129486 \
  --gas-report
forge snapshot --match-contract BaygusTradingVaultForkGasTest \
  --fork-url "$RPC" \
  --fork-block-number 25129486 \
  --snap ../benchmarks/snapshots/v2-mode-a-fork-25129486.gas-snapshot
```

`$RPC` was a mainnet RPC URL from the local environment.

## Snapshot Results

Local mock benchmark:

| Path | Gas |
| --- | ---: |
| Direct router buy to EOA | 83,249 |
| Vault buy to vault | 97,034 |
| Standalone approve | 28,731 |
| Direct router sell with preapproval | 45,656 |
| Vault Mode A emergency sell | 117,778 |
| Fee-on-transfer vault buy to vault | 121,537 |
| Fee-on-transfer vault Mode A emergency sell | 137,328 |

Mainnet fork benchmark:

- Block: `25,129,486`
- Router: Uniswap V2 `0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D`
- Routes: ETH/WETH <-> USDC; RFI fee-on-transfer fixture

| Path | Gas |
| --- | ---: |
| Direct router buy to EOA | 124,277 |
| Vault buy to vault | 141,789 |
| Standalone approve | 32,477 |
| Direct router sell with preapproval | 59,068 |
| Vault Mode A emergency sell | 112,092 |
| Fee-on-transfer RFI vault buy plus emergency sell | 346,040 |

## Deltas

Local mock:

```text
vault_buy_extra = 97,034 - 83,249 = 13,785
mode_a_sell_extra_vs_preapproved = 117,778 - 45,656 = 72,122
approve_plus_preapproved_sell_reference = 28,731 + 45,656 = 74,387
mode_a_vs_approve_plus_sell_reference = 117,778 - 74,387 = 43,391
fot_buy_extra_vs_plain_vault_buy = 121,537 - 97,034 = 24,503
fot_sell_extra_vs_plain_vault_sell = 137,328 - 117,778 = 19,550
```

Mainnet fork:

```text
vault_buy_extra = 141,789 - 124,277 = 17,512
mode_a_sell_extra_vs_preapproved = 112,092 - 59,068 = 53,024
approve_plus_preapproved_sell_reference = 32,477 + 59,068 = 91,545
mode_a_vs_approve_plus_sell_reference = 112,092 - 91,545 = 20,547
fot_rfi_buy_plus_sell = 346,040
```

The approve-plus-sell reference is a lower-bound execution comparison, not a
full two-public-transaction cost model. A real two-transaction flow also pays a
second transaction envelope/base fee and is nonce-blocked during an emergency.

## Interpretation

Mode A costs more than a sell with pre-existing allowance, as expected. On the
mainnet fork, the extra emergency-sell execution gas is about `53k` for the USDC
route.

Against a separate approve plus sell, Mode A is roughly competitive on gas and
strictly better operationally for scam exits because approval and sell are
atomic in one top-level transaction. It avoids the public two-tx race where sell
is nonce-blocked behind approval.

For gas-rank planning, the emergency sell route should use the fork number as
the first estimate:

```text
estimated_gas_used = 112,000 to 135,000 before adding live safety margin
```

Fee-on-transfer tokens are more expensive. The deterministic mock shows a local
Mode A emergency sell at `137,328` gas, and the RFI mainnet fork buy+sell smoke
test costs `346,040` gas at block `25,129,486`. Use a separate conservative
gas estimate for taxed-token exits rather than reusing the plain USDC path.

## Follow-Up

- Simulate exact buy and emergency-sell calldata with `tx_simulator`.
- Feed the measured emergency-sell gas into the live gas-rank provider.

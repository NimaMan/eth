# Mocks

Small test doubles for ERC20 tokens, fee-on-transfer ERC20 tokens, and the
Uniswap V2 router surface used by Mode A.

Mocks should model only behavior asserted by tests. Do not grow them into
partial protocol implementations.

# V4 Test Mocks

Mock Universal Router, Permit2, PoolManager, and hook contracts belong here.
Mocks should model only the behavior required by tests; do not let mock-only
behavior define production policy.

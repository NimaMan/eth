# src

Public live-state protocol crate.

The modules here define the shared data contracts and store traits used by the Ethereum alpha runtime. They must stay independent from concrete block processors, token managers, mempool processors, Redis clients, or trading-engine orchestration.

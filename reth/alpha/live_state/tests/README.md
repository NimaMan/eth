# tests

Compatibility and behavior tests for `eth_live_state`.

Key tests protect parity with the Python `eth_data.live_data_registry` and current Rust simulator helpers. Store tests verify the trait contract without needing Redis.

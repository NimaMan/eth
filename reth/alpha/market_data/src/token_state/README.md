# token_state

Token-state update boundary.

The concrete processor can be backed by `eth_token` and its live token cache. This module only defines the output contract: which token snapshots were updated or removed after a processed confirmed block.

# Run Template

Copy this folder to a date-stamped run folder before a rehearsal or deployment.

The run folder is the evidence package. It should contain only public data:
hashes, receipts, config values already visible on-chain, dry-run reports, and
operator signoff. Keep RPC URLs with credentials, bearer tokens, and private
keys out of the run folder.

# Mocks

Small test doubles for the PoolManager, ERC20 tokens, hook adapter, and V2 router.

Mocks should model only behavior asserted by tests. Do not grow them into partial protocol
implementations.
